# The 3749 App — Scaffold

This workspace contains the scaffold for The 3749 App as requested. Run the following to get started:

```bash
cd /home/pranav/optix/3749-dashboard
npm install
npm run dev
```

Environment variables required (minimal):

- `DATABASE_URL`
- `DIRECT_URL`
- `GOOGLE_CLIENT_ID`
- `GOOGLE_CLIENT_SECRET`
- `NEXTAUTH_SECRET`
# 3749-dashboard